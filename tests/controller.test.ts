// This test file is replaced by singleton.test.ts
// See singleton.test.ts for CityController (Singleton pattern) tests

describe('Redirect Notice', () => {
  test('Controller tests moved to singleton.test.ts', () => {
    expect(true).toBe(true);
  });
});
